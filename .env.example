# --- runtime mode ---
ENV=production            # or development
GIN_MODE=release          # release|debug|test

# --- server ---
PORT=8080
ALLOW_ORIGINS=https://your-frontend.com   # CSV if multiple: https://a.com,https://b.com
TRUSTED_PROXIES=0.0.0.0/0                 # or your LB CIDR(s), e.g. 10.0.0.0/8

# --- timeouts (Go ParseDuration format) ---
READ_TIMEOUT=10s
WRITE_TIMEOUT=15s
IDLE_TIMEOUT=60s

# --- security & db ---
DATABASE_URL=postgres://user:pass@host:5432/dbname?sslmode=require
JWT_SECRET=replace-me                       # required in production
